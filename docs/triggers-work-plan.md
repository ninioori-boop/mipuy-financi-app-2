# 📲 תוכנית עבודה — הטריגרים ("השומר")

**מטרה:** לבנות את החוליה האחרונה — ה"שולח" שמופעל אוטומטית בכל תשלום ושולח את העסקה
ל-`/api/transaction` (שכבר חי ומאומת). אנדרואיד דרך התראות, iOS דרך Shortcut.

**הקשר:** ה-backend המלא בנוי, פרוס, ומאומת E2E. ניקוי-האבטחה הושלם. זה כל מה שנשאר.

---

## 🛡️ עיקרון-העל: בידוד מוחלט מהמערכת החיה

הטריגר הוא **artifact נפרד לגמרי**. במקרה הגרוע הוא עצמו לא עובד — **הלקוחות החיים בשתי האפליקציות לא מושפעים בכלל.**

### ❌ 2 הדברים האסורים (היחידים שיכולים לשבור את החי):
1. **אסור** להוסיף Capacitor / קוד native ל-**repo הראשי** (`mipuy-financi-app-v2`) — עלול לשבור את build של Vercel. → הפרויקט ה-native יושב **נפרד לגמרי**.
2. **אסור** לשנות `next.config.ts` ל-static-export (`output: 'export'`) — ישבור את ה-API routes החיים. → משתמשים בגישת **WebView-טוען-URL** (האפליקציה ה-native רק טוענת את הכתובת החיה).

**כל עוד שומרים על ה-2 האלה — אי אפשר לשבור את המערכת החיה.**

### עיקרון נוסף — בלי לגעת בקוד הווב גם בשביל הטוקן:
האפליקציה ה-native תצטרך את טוקן-המכשיר. במקום להוסיף קוד-גשר לאפליקציית-הווב (זה כן נגיעה בחי), האפליקציה ה-native **תזריק JS לתוך ה-WebView** ששולף את הטוקן מהסשן המחובר (קורא ל-`/api/device-token`). כל הגשר חי בפרויקט ה-native בלבד → אפס נגיעה בווב.

---

## שלב 1 — 🍎 Shortcut ל-iOS (סיכון כולל: 🟢 אפס)
| # | צעד | בעלים | סיכון |
|---|-----|-------|:-----:|
| 1.1 | לכתוב מתכון Shortcut מלא (אוטומציית Wallet → "Get Contents of URL" POST ל-`/api/transaction` עם token + merchant + amount) | 🤖 | 🟢 |
| 1.2 | לתעד את ההקמה החד-פעמית ללקוח (הוספת Shortcut + הפעלת אוטומציה) | 🤖 | 🟢 |
| 1.3 | בדיקה: לקוח עם אייפון מקים + מקרב Apple Pay → רואים בטאב הוצאות | 👤 (לקוח עם אייפון) | 🟢 |

> הכל הוראות + API קיים. אין קוד, אין build, לא נוגע בכלום.

---

## שלב 2 — 🤖 אפליקציית אנדרואיד native (סיכון לווב: 🟢 אפס אם מבודד)
ארכיטקטורה: פרויקט **Capacitor נפרד** → WebView טוען את הכתובת החיה + פלאגין מאזין-התראות native.
| # | צעד | בעלים | סיכון |
|---|-----|-------|:-----:|
| 2.1 | להקים פרויקט Capacitor ב-**תיקייה/repo נפרד** (לא הראשי!). `capacitor.config` → `server.url` = הכתובת החיה | 🤖 | 🟢 לווב |
| 2.2 | פלאגין `NotificationListenerService` (Kotlin) — קורא התראות מ-Google Pay / אפליקציית הבנק | 🤖 | 🟢 לווב |
| 2.3 | מנתח שם-עסק + סכום מטקסט ההתראה (regex/היוריסטיקה לכל אפליקציה) — **דורש דוגמת-התראה אמיתית מהטלפון שלך** | 🤖 + 👤 | 🟢 |
| 2.4 | גשר-טוקן: הזרקת JS ל-WebView ששולפת את הטוקן ומעבירה ל-native (בלי לגעת בקוד הווב) | 🤖 | 🟢 |
| 2.5 | ה-native עושה POST ל-`/api/transaction` עם `{token, merchant, amount}` | 🤖 | 🟢 |

> הסיכון היחיד: האפליקציה ה-native עצמה לא עובדת → מתקנים. הווב החי לא נוגע.

---

## שלב 3 — 📦 Build בענן (סיכון: 🟢 אפס-נמוך)
כי אי אפשר להריץ כלי-אנדרואיד מקומית — בונים APK בענן.
| # | צעד | בעלים | סיכון |
|---|-----|-------|:-----:|
| 3.1 | workflow GitHub Actions (ב-repo ה-native הנפרד) שבונה APK חתום | 🤖 | 🟢 |
| 3.2 | להוריד את ה-APK + sideload (להפעיל "התקנה ממקורות לא מוכרים") | 👤 | 🟢 |

---

## שלב 4 — 🧪 בדיקה על הטלפון שלך (סיכון: 🟢 אפס)
| # | צעד | בעלים |
|---|-----|-------|
| 4.1 | להתקין את ה-APK | 👤 |
| 4.2 | לאשר הרשאת-גישה-להתראות | 👤 |
| 4.3 | להתחבר (WebView) → הטוקן נשלף לבד | 👤 |
| 4.4 | לשלם Google Pay אמיתי → לוודא שזה נכנס לטאב הוצאות | 👤 |

---

## שלב 5 — 🎨 הפצה ללקוחות (מאוחר יותר)
| # | צעד |
|---|-----|
| 5.1 | ליטוש onboarding (מסך "הפעל מעקב" + מדריך מצויר) |
| 5.2 | Google Play internal testing → שחרור |
| 5.3 | iOS: יצירת קישור-Shortcut מותאם-לקוח בתוך האפליקציה |

---

## דרישות מקדימות / החלטות פתוחות
- **כתובת שהאפליקציה תטען:** כרגע `mipuy-financi-app-2-3nay.vercel.app` (staging). כשתעלה לדומיין production → לעדכן.
- **דוגמת-התראה של Google Pay** מהטלפון שלך — כדי לכתוב את הפרסר (צעד 2.3).
- **repo נפרד** לפרויקט ה-native.
- **מפתח-חתימה לאנדרואיד** (לחתום על ה-APK) — נוצר, נשמר סודי.

## חלוקת בעלות
- 🤖 **אני:** כל הקוד (פרויקט Capacitor, פלאגין, CI, מסמכים).
- 👤 **אתה:** דוגמת-התראה, התקנת APK, אישור הרשאה, בדיקה, צעדי Play console.

## סדר מומלץ
**שלב 1 (iOS Shortcut) קודם** — אפס סיכון, דליברבל מיידי, מלמד את הזרימה. במקביל מתחילים את תשתית האנדרואיד (שלב 2-3).

_נכתב: 2026-06-20._
