# 🍎 תיעוד אוטומטי של Apple Pay באייפון — הקמת Shortcut

מדריך חד-פעמי. אחרי ההקמה, **כל תשלום Apple Pay בחנות נרשם לבד** בטאב תיעוד ההוצאות.

## דרישות
- iPhone עם **iOS 17 ומעלה** (אז נוספה אוטומציית "Transaction"/"Wallet").
- Apple Pay מוגדר עם כרטיס.

## מה זה תופס / לא תופס
- ✅ תשלומי **Apple Pay פיזיים בחנות** (קירוב NFC).
- ❌ **לא** קניות אונליין, ולא כרטיס פיזי רגיל (בלי Apple Pay).

---

## ההקמה — 3 שלבים

### שלב 1 — קבל את הטוקן והוסף את ה-Shortcut
1. ב-iPhone, פתח ב-Safari את:
   ```
   https://mipuy-financi-app-2-3nay.vercel.app/connect
   ```
   *(או הדומיין `https://app.orimipuy.com/connect` — אותו שרת.)*
2. התחבר עם **המייל והסיסמה של הכלכלן של הבית** (או Google).
3. לחץ **"📋 העתק טוקן"** (שלב 1 במסך) — הטוקן עכשיו בלוח.
4. לחץ **"📲 הוסף את ה-Shortcut"** (שלב 2). אפליקציית Shortcuts תיפתח עם מסך הוספה.
5. במהלך ההוספה תופיע שאלה — **הדבק שם את הטוקן** (הוא כבר בלוח, לחיצה ארוכה → Paste) → **הוסף Shortcut**.

> ה-Shortcut שנוסף נקרא **Mipuy**. הוא כבר יודע לאן לשלוח ועם איזה טוקן — לא צריך לגעת בו יותר.

### שלב 2 — צור את אוטומציית ה-Wallet (שתפעיל את ה-Shortcut אוטומטית)
> את החלק הזה אפל מחייבת להגדיר ידנית פעם אחת — אי אפשר לייבא אותו מוכן.

1. פתח את אפליקציית **Shortcuts** (קיצורים) → לשונית **Automation** (אוטומציה).
2. לחץ **+** (למעלה-ימין) → **Create Personal Automation**.
3. גלול ובחר **Transaction** (ב-iOS 26+ זה נקרא **Wallet**).
4. תחת **When I tap** — בחר את **הכרטיס/ים** שתרצה לעקוב אחריהם (ואפשר Category = All) → **Next**.
5. לחץ **Add Action** → חפש **"Run Shortcut"** ובחר אותו.
6. בפעולה שנוספה לחץ על המילה **"Shortcut"** ובחר את **Mipuy**.
7. ודא שהקלט של ה-Transaction מועבר ל-Shortcut (כברירת מחדל זה כך — ה-"Shortcut Input").
8. לחץ **Next** → **כבה** את **Ask Before Running** (כדי שלא יבקש אישור בכל תשלום) → **Done**.

### שלב 3 — בדיקה
שלם עם Apple Pay בחנות (קירוב). תוך כמה שניות — פתח את האפליקציה → טאב **תיעוד הוצאות** → התשלום אמור להופיע שם, מקוטלג. ✅

---

<details>
<summary><b>אם הייבוא לא עובד — הקמה ידנית מלאה (fallback)</b></summary>

אם מסיבה כלשהי ה-Shortcut המוכן לא נוסף או לא רץ, אפשר לבנות את כל הלוגיקה **ישירות בתוך האוטומציה**, בלי לייבא כלום:

#### חלק א׳ — יצירת האוטומציה
1. **Shortcuts** → **Automation** → **+** → **Create Personal Automation**.
2. בחר **Transaction** (או **Wallet**).
3. **When I tap** — בחר כרטיס/ים (או Category = All).
4. סמן **Run Immediately** → **Next**.

#### חלק ב׳ — הפעולה (שליחה לשרת)
5. **Add Action** → חפש **"Get Contents of URL"** ובחר אותו.
6. בשדה ה-URL הדבק:
   ```
   https://mipuy-financi-app-2-3nay.vercel.app/api/transaction
   ```
7. לחץ **"Show More"** מתחת ל-URL:
   - **Method:** **POST**
   - **Headers:** Add header → Key = `Content-Type`, Value = `application/json`
   - **Request Body:** **JSON**
8. תחת Request Body (JSON) הוסף 3 שדות (**Add new field**):
   | Key | Type | Value |
   |-----|------|-------|
   | `token` | Text | *(הדבק כאן את טוקן-המכשיר שלך מ-/connect)* |
   | `merchant` | Text | המשתנה **Merchant** (בחר מתוך Shortcut Input) |
   | `amount` | Number | המשתנה **Amount** |
9. **Next** → **Done**.

> אם הסכום נכנס מוזר (עם סימן מטבע): לפני שלב 5 הוסף פעולה **"Get Numbers from Input"** על המשתנה Amount, והשתמש בתוצאה שלה בשדה `amount`.

</details>

---

## 🔔 שלב 4 (אופציונלי) — התראה על כל קליטה, כמו באנדרואיד

השרת מחזיר בתשובת ה-POST אובייקט `notify` עם כותרת וגוף מוכנים
("נרשם: שופרסל · ₪120" + סטטוס התקציב של הקטגוריה, כולל ⚠️ בחריגה).
כדי שהאייפון יציג את זה כהתראה, מוסיפים **3 פעולות** מיד **אחרי**
"קבלת התוכן של כתובת אינטרנט" (בתוך האוטומציה או בתוך קיצור ה-Mipuy — איפה שה-POST נמצא):

1. **＋ פעולה** → חפש **«מילון»** → בחר **"קבלת ערך מהמילון"** (Get Dictionary Value).
   - במפתח (השדה הריק) הקלד: `notify.title`
   - הקלט אמור להתחבר לבד ל**"תוכן כתובת האתר"** (התוצאה של פעולת ה-POST).
2. **＋ פעולה** → שוב **"קבלת ערך מהמילון"**:
   - מפתח: `notify.body`
   - קלט: שוב "תוכן כתובת האתר" (אם התחבר לבד ל"ערך מילון" של הפעולה הקודמת —
     הקש על המשתנה והחלף ל"תוכן כתובת האתר").
3. **＋ פעולה** → חפש **«התראה»** → בחר **"הצגת התראה"** (Show Notification):
   - בגוף ההתראה (שדה הטקסט הראשי): בחר משתנה → **"ערך מילון"** של פעולה **2** (ה-body).
   - הקש על החץ/״הצג עוד״ לפתיחת שדה **"כותרת"** → בחר משתנה → **"ערך מילון"** של פעולה **1** (ה-title).

> שתי פעולות המילון יוצרות משתנים בשם זהה ("ערך מילון") — ההבדל הוא הסדר:
> הראשון = כותרת, השני = גוף. אם מתבלבלים, לוחצים על המשתנה ורואים איזה מפתח הוא שולף.

> ⚠️ **לא לגעת בפעולת "קבלת התוכן של כתובת אינטרנט" עצמה** — עריכה חוזרת שלה
> עלולה להשחית אותה (שגיאת ‎-1005, ראה סטטוס למטה). רק מוסיפים פעולות אחריה.

> הערה: פעולת "הצגת התראה" עם key-path כמו `notify.title` נתמכת ישירות —
> אם משום מה המפתח המקונן לא עובד, מפצלים לשתיים: קודם "קבלת ערך מהמילון"
> עם מפתח `notify`, ואז ערך מהמילון עם מפתח `title` על התוצאה.

תשלום שנכשל (טוקן שגוי וכו') לא יציג התראה שימושית — התשובה בלי `notify` → התראה ריקה או כלום. זה בסדר: ההתראה היא אישור הצלחה, לא ערוץ שגיאות.

---

## 🔒 הערת אבטחה
טוקן-המכשיר מאפשר רק **להזריק הוצאות לחשבון שלך** (לא לקרוא נתונים). עדיין — אל תשתף אותו.

## 📌 ליועץ (Ori) — סטטוס
- **2026-07-09: ה-Shortcut נבנה ידנית על אייפון אמיתי ושותף ב-iCloud** (מסלול
  החתימה ב-CI ננטש — אפל מסרבת לחתום בלי מסך). הקישור:
  `https://www.icloud.com/shortcuts/859eb60273434daaa96c1a7fe64cbb2e`
  והוא מחווט לכפתור ההוספה ב-`/connect`.
- **מבנה הקיצור בפועל:** מלל («הדבק כאן טוקן») → קבלת התוכן של
  `https://app.orimipuy.com/api/transaction` (POST, JSON) עם שדות
  `token` (=המלל) ו-`merchant` (=קלט של קיצור, כטקסט מלא). **אין שדה amount** —
  השרת מפענח לבד סכום+שם עסק מהטקסט הגולמי (extractFromRaw ב-/api/transaction),
  כי עורך הקיצורים לא מציע סוג "עסקה" לחילוץ מאפיינים.
- **הגדרה אצל לקוח:** /connect (שלבים 1-4 בעמוד): העתקת טוקן → הוספת הקיצור →
  הדבקת הטוקן בתיבת המלל → יצירת אוטומציית עסקה → הוספה למסך הבית.
- **2026-07-11: אומת מקצה-לקצה על האייפון של אורי (iOS 26)** — תשלום Apple Pay
  אמיתי נקלט בתיעוד ההוצאות, מקוטלג. המתכון המלא (כולל תקלת ‎-1005 והשמות
  החדשים ב-iOS 26: "פעולות אוטומטיות"/"ארנק"/"כמות") — בזיכרון הפרויקט.
- **2026-07-12: נוסף שלב 4 (התראה)** — השרת כבר החזיר `notify{title,body,warn}`
  מאז גרסת האנדרואיד; ההוראות למעלה מוסיפות את הצגתה בקיצור. אחרי אימות על
  האייפון של אורי — לעדכן גם את הקיצור המשותף ב-iCloud (בנייה מחדש בטלפון →
  קישור שיתוף חדש → החלפת `SHORTCUT_ICLOUD_URL` ב-`src/app/connect/page.tsx`).

_עודכן: 2026-07-12._
